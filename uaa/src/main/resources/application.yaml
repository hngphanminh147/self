logging:
  level:
    root: INFO

spring:
  profiles:
    active: dev
  web:
    resources:
      add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true

server:
  servlet:
    encoding:
      force: true